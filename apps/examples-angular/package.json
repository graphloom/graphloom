{
  "name": "examples-angular",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "dependencies": {
    "@graphloom/angular": "workspace:*",
    "@graphloom/core": "workspace:*",
    "@graphloom/interaction": "workspace:*"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/main.server.ts --outDir dist/server && node prerender.mjs",
    "preview": "vite preview",
    "serve": "pnpm build && vite preview --port 4301 --strictPort",
    "lint": "eslint src",
    "typecheck": "tsc -b"
  }
}
